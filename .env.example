# Copy to .env and fill in. .env is gitignored — never commit the real file.
#
# Your Polymarket wallet address (the proxy/funder address, 0x + 40 hex chars).
# Read-only: this app only ever GETs public position data. It never signs,
# never trades, and never needs a private key. If any code or doc ever asks you
# for a PRIVATE key or seed phrase, that is not this project. Do not paste it.
POLYMARKET_WALLET_ADDRESS=0x0000000000000000000000000000000000000000
